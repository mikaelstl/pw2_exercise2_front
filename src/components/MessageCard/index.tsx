import { Container } from "./style";

interface MessageCardProps {
  className: string
}

export function MessageCard(props: MessageCardProps) {
  return (
    <Container className={props.className}>daknednakn</Container>
  )
}